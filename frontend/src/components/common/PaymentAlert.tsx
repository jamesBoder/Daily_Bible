import React from 'react';

// PaymentAlert is no longer rendered — subscriptions replaced by lifetime purchase.
// File kept to avoid broken imports. Safe to delete in a future pass.
export const PaymentAlert: React.FC = () => null;
